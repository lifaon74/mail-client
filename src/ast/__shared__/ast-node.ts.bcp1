import { IAstNode } from './ast-node.type';

export function AstNode<GType extends string, GExtra extends object>(
  type: GType,
  extra?: GExtra,
): GExtra & IAstNode<GType> {
  return {
    ...extra,
    __type__: type,
  } as GExtra & IAstNode<GType>;
}
